name: eccomerce_shop_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# I should update this version each time I make a release of my app
version: 1.0.0+1

environment:
  sdk: '>=2.19.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_iconly: ^1.0.2
  card_swiper: ^3.0.1
  page_transition: ^2.0.9
  provider: ^6.0.5
  fancy_shimmer_image: ^2.0.2
  http: ^1.3.0
  cached_network_image: ^3.4.1
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  win32: ^5.11.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

